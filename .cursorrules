# Litecoin Fund

Canonical Cursor rules for this app: **[`.cursor/rules/litecoin-fund.mdc`](../.cursor/rules/litecoin-fund.mdc)** (monorepo root).

Open the **Migrate** workspace root so Cursor loads all `.cursor/rules/*.mdc` files.
